name: 'srikanth Learning giphy PR Comment'
description: 'Add a giphy GIF comment to new pull requests'
inputs:
  github-token:
    description: 'Github Token'
    required: true
  giphy-api-key:
    description: 'Giphy API Key'
    required: true

runs:
  using: 'node20'
  main: 'dist/index.js'
branding:
  icon: 'award'
  color: 'green'
